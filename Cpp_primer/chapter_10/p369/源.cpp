//10_41
//1.在beg到end范围中，将old_val替换为new_val
//2.在beg到end范围中，若有使得pred为真的元素，再将其替换为new_val
//3.在beg到end范围中，将old_val替换为new_val，将一切结果存入dest迭代器所指的容器
//4.在beg到end范围中，若有使得pred为真的元素，则将其替换为new_val，将一切结果存入dest迭代器所指的容器