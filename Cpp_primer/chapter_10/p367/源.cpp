//10_38
//输入迭代器:只支持顺序访问；支持前置和后置递增；提供==，！=判断计算；可以进行解引用操作。
//输出迭代器:支持前置和后置递增运算；支持解引用操作。
//前向迭代器:支持所有输入输出迭代器操作，但只能向一个方向移动。
//双向迭代器:支持所有输入输出迭代器操作，还可以使用前置和后置递减操作，可向前后移动。
//随机访问迭代器:支持所有双向迭代器操作，可随机访问元素。

//10_39
//list，双向迭代器。
//vector，随机访问迭代器。

//10_40
//copy，第一形参为输入迭代器，第二形参为输入迭代器，第三形参为输出迭代器
//reverse，前向迭代器
//unique，双向迭代器